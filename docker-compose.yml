version: "3.8"

services:
  sync:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - API_KEY=${API_KEY:-}
      - MAX_PAYLOAD_MB=${MAX_PAYLOAD_MB:-10}
    volumes:
      - sync-data:/app/data
    restart: unless-stopped

volumes:
  sync-data:
